const { test, expect } = require('@playwright/test');


test('Verify Subjects on Home Page', async ({ page }) => {

    await page.goto('https://www.makemytrip.com/');
    // await expect(page.locator('[aria-label="View All Journals"]')).toContainText(['  All Journals']);
    // await page.locator('[data-testid="quick-search-link"]').nth(0).click();
    // await expect(page.locator('[aria-label="View All Journals"]')).toContainText(['  All Journals']);
    // await expect(page.locator('[aria-label="View All Journals"]')).toContainText(['  All Journals']);
    // await expect(page.locator('[aria-label="View All Journals"]')).toContainText(['  All Journals']);
    // await expect(page.locator('[aria-label="View All Journals"]')).toContainText(['  All Journals']);
    // await expect(page.locator('[aria-label="View All Journals"]')).toContainText(['  All Journals']);

    
  });